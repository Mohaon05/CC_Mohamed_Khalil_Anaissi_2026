@page
@model trabalho_kaneko.Pages.PaisesEditarModel
@{
    ViewData["Title"] = "Editar País";
}

<div class="container mt-4">
    <div class="mb-3">
        <a asp-page="/PaisesListar" class="btn btn-secondary btn-sm">Voltar para a Lista</a>
    </div>

    <div class="card shadow-sm border-warning">
        <div class="card-header bg-warning text-dark fw-bold">
            <h4 class="mb-0">Editar País</h4>
        </div>
        <div class="card-body">

            <form method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <input type="hidden" asp-for="Pais.IdPais" />

                <div class="row g-3">
                    <div class="col-md-9">
                        <label class="form-label fw-bold">Nome do País</label>
                        <input asp-for="Pais.Pais" class="form-control" required maxlength="60" />
                    </div>

                    <div class="col-md-3">
                        <label class="form-label fw-bold">Sigla</label>
                        <input asp-for="Pais.Sigla" class="form-control" required maxlength="3" style="text-transform: uppercase;" />
                    </div>
                </div>

                <div class="mt-4 text-end">
                    <button type="submit" class="btn btn-warning px-4 fw-bold">Atualizar País</button>
                </div>
            </form>
        </div>
    </div>
</div>